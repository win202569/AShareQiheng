# SDD ledger — plan: docs/superpowers/plans/2026-08-29-seven-dimension-feature-foundation.md

Setup: isolated worktree `feature/seven-dimension-foundation` at `D:\Projects\AShareQiheng\.worktrees\seven-dimension-foundation`.
Setup: implementation base `e361fbd`; user-owned runtime data remains only in the main checkout.
Setup: baseline `109 tests in 12.101s — OK` using the existing project virtual environment with worktree-local TEMP/TMP.

Operational acceptance resumed on local `main@29bac16` (2026-09-01) after the user said `继续`. Read-only preflight found an exact, healthy v2 database (`quick_check=ok`, `integrity_check=ok`) with 120 current candidates matching 120 pending legacy `deep_financial` parents, 0/360 current `deep_statement` child slots, no active/expired running lease, no v3 tables, and stale pre-cutoff status generated 2026-08-29. The existing user-owned checkpoint paths are `data/state.sqlite3`, `data/status/pipeline_status.json`, `data/status/quality.json`, and one 5,550-row disclosure snapshot under `data/raw/akshare/disclosure_schedule/2026-08-29/`; SQLite WAL is zero bytes. Ruling: run no orchestrator command before a path-specific Git checkpoint because even `status` calls `StateStore.initialize()` and would migrate/write the database. Only the four named data path groups may be staged; `.superpowers`, code, WAL/SHM, and all unrelated content remain excluded. Cost if wrong: the checkpoint could preserve stale pre-cutoff data, but it is an intentional recoverable pre-migration baseline rather than an acceptance result; fresh post-cutoff collection and deep progress must be committed separately.

Operational checkpoint A complete: commit `2e9307f` (`data: checkpoint 2026-08-29 incremental crawl`) contains exactly the four verified data files/groups and excludes `.superpowers`, code, and SQLite WAL/SHM. Post-commit `data/` diff is empty. Ruling: execute a fresh post-cutoff `incremental --online` before any deep smoke, then checkpoint its new candidate context. Running deep first would expand the stale Aug-29 parents into Sep-1 child keys whose first-enqueue provenance can survive a later same-day candidate rebuild. Cost if wrong: the incremental stage makes four bounded source-stage calls and may update candidate membership before the one-statement smoke, but avoids mixing stale and refreshed same-date child provenance.

Post-cutoff incremental and checkpoint B complete: `incremental --online` exited 0 after four successful source stages (BaoStock security master + exact 2021-01-01..2026-09-11 calendar, AKShare disclosure schedule + performance report), with zero source errors and zero circuit breakers. SQLite migrated transactionally to v3 with migration ledger `(2,3)` and `quick_check=ok`. Curated quality is `data_ready`, cutoff passed, latest announcement date is 2026-08-31, candidate count remains 120, and five candidates were replaced. All 360 Sep-1 statement child jobs bind the same current candidate hash `f897a22b...c6fdaf6`, match current per-security performance hashes, and appear exactly three per member. Four new snapshots passed public repository verification; no `.part` file remained. Commit `e372ba5` (`data: checkpoint 2026-09-01 post-cutoff crawl`) contains exactly 11 verified data paths and excludes private/sidecar content. Coverage remains 0/120 before deep work; formal/seven-dimension gates are false and both official pools are zero.

Exact online deep smoke (`--limit 1`) completed one remote balance-sheet attempt for `SH600019` and then stopped before any wider batch because the job became `terminal_failed/malformed_statement`, producing `unknown_failure_count=1`. The persisted 104-row snapshot and SQLite both verify cleanly, no `.part` remains, but normalization recorded `statement_report_date_invalid`, wrote zero financial facts, and produced an immutable blocked feature bundle. Systematic root-cause tracing proved all 104 raw `REPORT_DATE` values use AKShare's stable `YYYY-MM-DD 00:00:00` form, while commit `17f5865` changed the fact parser from permissive slicing to date-only full-match. The same format exists in the Aug-28 snapshot, so this is a parser/source-contract regression rather than a Sep-1 upstream change. Offline falsification: original snapshot yields 0 facts + 1 error; changing only the 104 report-date lexical forms in memory yields 1,626 facts + 0 issues. Ruling: keep all smoke artifacts and the old quality issue, run no further online statement batch, and repair through a versioned fact mapping plus narrowly guarded offline reconciliation rather than deleting data, generic requeue, or hiding the unknown failure.

Repair isolation created at `D:\Projects\AShareQiheng\.worktrees\report-date-repair` on `codex/report-date-repair@e372ba5`. With worktree-local TEMP/TMP, the unmodified post-cutoff baseline runs 426 tests: 425 pass and one pre-existing/current-data test fails because the refreshed candidate set replaced industry `自动化设备` with previously unregistered `综合Ⅱ`. The design requires every current source label to be explicit and permits `unclassified`; this current-candidate registry update must be handled as a separate versioned change, not hidden as part of the date parser.
