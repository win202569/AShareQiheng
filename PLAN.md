# 沪深A股2026中报增量采集与评分执行计划

## 目标

在无 Tushare Token 的前提下，建立可中断恢复、可增量更新、可审计的数据管线：

- 报告期：2026-06-30。
- 公告截止：2026-08-31 23:59:59 Asia/Shanghai。
- 行情锚点：2026-08-31 收盘；最终冻结：2026-09-01 盘前。
- 范围：沪深主板、创业板、科创板 A 股，不含北交所。
- 当前日期早于截止日时只生成 provisional 预评分；最终池只能来自 final 冻结运行。

## 全局约束

- 巨潮/交易所公告和 PDF 是披露与财务事实的权威证据；AKShare/东方财富是结构化提取层；BaoStock 是证券主表和行情主源。
- 不绕过验证码、反爬或访问控制；403、429、验证码或异常 HTML 触发熔断。
- 所有时间内部以 UTC 保存，同时保留 Asia/Shanghai 业务时间。
- 原始数据不可覆盖；版本以内容 SHA-256 区分。
- 数据不足不填 0，评分覆盖不足时输出 blocked/partial 及原因。
- 每次运行必须持久化进度、包版本、参数、行数、错误和质量检查结果。
- 免费源核心字段完整率低于 95% 时，不发布正式全市场排名。

## Task 1：状态库与增量状态机

以 Python 标准库实现 SQLite 状态库，包含 run、job、artifact、source_snapshot、score_item、quality_issue 表；提供幂等 job 入队、租约领取、成功/重试/终止、过期租约回收、内容哈希去重、provisional/final 发布闸门和 JSON 进度快照。先写 unittest 并观察失败，再实现。

## Task 2：免费数据源适配与试采

实现可选依赖适配器：BaoStock 获取证券主表、交易日历及不复权日线；AKShare 获取2026半年报预约/实际披露、全市场业绩报表、东财三表和巨潮半年报公告元数据。先用全市场业绩报表形成预评分与约120只候选，再仅对候选抓完整三表，避免在截止日前逐股全量调用。适配器只返回规范化记录，外部访问与状态库解耦。实现 5–6 只代表性股票试采、低频限速、重试、空结果语义、源版本记录和交叉核验报告。

## Task 3：增量编排与评分占位

实现 CLI：bootstrap、incremental、status、finalize。bootstrap 建证券宇宙与初始任务；incremental 只抓取新增/变化公告、未完成财务和最新已完成交易日；finalize 只有在截止时间、8/31行情和质量闸门通过时才创建 final 冻结，否则失败且不覆盖 provisional。评分引擎先实现输入哈希、覆盖率、状态和重算选择，不在财务数据未齐时生成虚假总分。

## Task 4：进度报告与自动运行

生成机器可读 JSON 和用户可读 HTML/Markdown 状态报告，展示股票宇宙、已披露、三表就绪、冲突、失败、预评分、数据新鲜度和下一次动作。创建线程心跳：8月29日至31日夜间增量运行；9月1日清晨执行最终冻结和评分。

## 验收

- 单元测试覆盖幂等、失败恢复、截止日、未来日期、重复公告、版本变化、缺失值、质量闸门与 final 不可覆盖。
- 试采能够在中断后续跑，不重复提交成功任务。
- 所有最终记录可追溯到输入快照、规则版本和内容哈希。
- 自动化运行提示明确引用本计划与管线路径，不创建重复任务。
